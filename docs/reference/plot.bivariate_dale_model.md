# plot() overloading for bivariate_dale_model

plot() overloading for bivariate_dale_model

## Usage

``` r
# S3 method for bivariate_dale_model
plot(x, ...)
```

## Arguments

- x:

  the bivariate_dale_model object

- ...:

  arbitrary params including y1, y2 (label for predictors), plot_type
  ("ci" or "sp")

## Value

patchwork object
