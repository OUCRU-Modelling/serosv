# Monotonize seroprevalence

Monotonize seroprevalence

## Usage

``` r
pava(pos = pos, tot = rep(1, length(pos)))
```

## Arguments

- pos:

  the positive count vector.

- tot:

  the total count vector.

## Value

computed list of 2 items pai1 for original values and pai2 for
monotonized value
