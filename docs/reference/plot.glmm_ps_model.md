# plot() overloading for penalized spline fitted with GLMM

plot() overloading for penalized spline fitted with GLMM

## Usage

``` r
# S3 method for glmm_ps_model
plot(x, ...)
```

## Arguments

- x:

  the glmm_ps_model object

- ...:

  arbitrary params.
