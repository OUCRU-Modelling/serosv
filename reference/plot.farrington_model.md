# plot() overloading for Farrington model

plot() overloading for Farrington model

## Usage

``` r
# S3 method for farrington_model
plot(x, ...)
```

## Arguments

- x:

  the Farrington model object.

- ...:

  arbitrary params.

## Value

ggplot object
